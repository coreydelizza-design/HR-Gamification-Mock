{
  "name": "fieldguide",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Fieldguide — the organization-first collaboration-readiness platform. Organization cards, cross-org dependencies, success agreements, meeting fit, and a collaboration map. Org charts show reporting structure; Fieldguide shows working structure.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
